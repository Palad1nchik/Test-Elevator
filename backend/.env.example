# Runtime environment: development | production
APP_ENV=development

# Bind address
HOST=0.0.0.0
PORT=8080

# Comma-separated list of browser origins allowed via CORS.
# In production set this to your domain, e.g. https://kasper.kz
CORS_ORIGINS=http://localhost:5173
